apiVersion: valkey.andino.io/v1alpha1
kind: Valkey
metadata:
  name: valkey-standalone-ssl
spec:
  architecture: standalone
  tls:
    enabled: true
    autoGenerated: false
    existingSecret: valkey-single-tls-cert
    certFilename: "tls.crt"
    certKeyFilename: "tls.key"
    certCAFilename: "ca.crt"
  auth:
    enabled: true
    password: test_pwd

