apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "valkeybackup.fullname" . }}-test-connection"
  labels:
    {{- include "valkeybackup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "valkeybackup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
