apiVersion: v1
kind: Service
metadata:
  name: {{ include "valkeybackup.fullname" . }}
  labels:
    {{- include "valkeybackup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "valkeybackup.selectorLabels" . | nindent 4 }}
